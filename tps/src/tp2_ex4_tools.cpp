#include "../include/tp2_ex4_tools.h"

#include <pmp/algorithms/DifferentialGeometry.h>

void printBoundingBoxCoords(pmp::SurfaceMesh& mesh)
{
    /* ! STUDENTS TODO ! */
    // Print the coordinates of the bounding box extremum
}

void printBoundingBoxSize(pmp::SurfaceMesh& mesh)
{
    /* ! STUDENTS TODO ! */
    // Print the lengths of the bounding box (in x, y, and z directions)
}

void printBoundingBoxDiag(pmp::SurfaceMesh& mesh)
{
    /* ! STUDENTS TODO ! */
    // Print the length of the diagonal of the bounding box
}

void printMeshTotalArea(pmp::SurfaceMesh& mesh)
{
    /* ! STUDENTS TODO ! */
    // Print the area of the mesh
}

void printMeshVerticesCentroid(pmp::SurfaceMesh& mesh)
{
    /* ! STUDENTS TODO ! */
    // Print the centroid of the vertices
}

void printMeshVolume(pmp::SurfaceMesh& mesh)
{
    /* ! STUDENTS TODO ! */
    // Print the volume of the mesh (see how it is computed)
}

void printEulerNumberAndGenus(pmp::SurfaceMesh& mesh)
{
    /* ! STUDENTS TODO ! */
    // Print the Euler caracteristic and the genus of the mesh
}